- platform: command_line
  switches:
    radio_person1:
      command_on: "/local/bin/squeezy/alarms -r person1 -a alarm_on"
      command_off: "/local/bin/squeezy/alarms -r person1 -a alarm_off"
      command_state: "/local/bin/squeezy/alarms -r person1 -a alarm_state"
      friendly_name: person1's radio alarm
    radio_person2:
      command_on: "/local/bin/squeezy/alarms -r person2 -a alarm_on"
      command_off: "/local/bin/squeezy/alarms -r person2 -a alarm_off"
      command_state: "/local/bin/squeezy/alarms -r person2 -a alarm_state"
      friendly_name: person2's radio alarm
