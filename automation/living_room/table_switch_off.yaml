initial_state: 'off'
alias: 'Table switch off'
trigger:
  platform: state
  entity_id: switch.table_light_switch
  to: 'off'
  for:
    seconds: 10
action:
  - service: switch.turn_on
    entity_id: switch.table_light_switch
